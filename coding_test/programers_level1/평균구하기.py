# 배열(리스트) 안에 있는 요소들의 평균값 구하기
# 내가 푼 문제
def solution(arr):
    return sum(arr) / len(arr)

# 다른사람이 푼 문제 (난 예외 생각 안함 ㅎㅎ)
def solution(arr):
    if len(arr) <= 0:
        return 0
    else:
        return sum(arr) / len(arr)
        

